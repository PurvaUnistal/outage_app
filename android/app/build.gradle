plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
    id "com.google.gms.google-services"
}

def localProperties = new Properties()
def localPropertiesFile = rootProject.file('local.properties')
if (localPropertiesFile.exists()) {
    localPropertiesFile.withReader('UTF-8') { reader -> localProperties.load(reader) }
}

def keystoreProperties = new Properties()


android {
    namespace "unistal"
    compileSdk 36
    ndkVersion = "28.2.13676358"

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions { jvmTarget = '17' }
    sourceSets { main.java.srcDirs += 'src/main/kotlin' }

    flavorDimensions "app"

    productFlavors {
        prodAGCL {
            dimension "app"
            applicationIdSuffix ".agcloutage.app"
            resValue "string", "app_name", "AGCL Outage"
            manifestPlaceholders = [
                    appIcon: "@mipmap/agcl",
                    appIconRound: "@mipmap/agcl"
            ]
            def keystorePropertiesFile = rootProject.file('key.properties')
            if (keystorePropertiesFile.exists()) {
                keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
            }
            versionCode 23
            versionName "1.0.2-AGCL"
        }
        prodMGL {
            dimension "app"
            applicationIdSuffix ".mgloutage.app"
            resValue "string", "app_name", "MGL Outage"
            manifestPlaceholders = [
                    appIcon: "@mipmap/mgl_logo",
                    appIconRound: "@mipmap/mgl_logo"
            ]
            versionCode 01
            versionName "1.0.0-MGL"
        }
        prodPBGPL {
            dimension "app"
            applicationIdSuffix ".pbgploutage.app"
            resValue "string", "app_name", "PBGPL Outage"
            manifestPlaceholders = [
                    appIcon: "@mipmap/pbg_logo",
                    appIconRound: "@mipmap/pbg_logo"
            ]
            def keystorePropertiesFile = rootProject.file('pbgpl_key.properties')
            if (keystorePropertiesFile.exists()) {
                keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
            }
            versionCode 05
            versionName "1.2.0-PBGPL"
        }
        prodIGL {
            dimension "app"
            applicationIdSuffix ".igloutage.app"
            resValue "string", "app_name", "IGL Outage"
            manifestPlaceholders = [
                    appIcon: "@mipmap/unistal_logo",
                    appIconRound: "@mipmap/unistal_logo"
            ]
            versionCode 01
            versionName "1.0.0-IGL"
        }
        prodHPOIL {
            dimension "app"
            applicationIdSuffix ".hpoiloutage.app"
            resValue "string", "app_name", "HPOIL Outage"
            manifestPlaceholders = [
                    appIcon: "@mipmap/unistal_logo",
                    appIconRound: "@mipmap/unistal_logo"
            ]
            def keystorePropertiesFile = rootProject.file('hpoil_key.properties')
            if (keystorePropertiesFile.exists()) {
                keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
            }
            versionCode 01
            versionName "1.0.0-HPOIL"
        }
        prodHPCL {
            dimension "app"
            applicationIdSuffix ".hpcloutage.app"
            resValue "string", "app_name", "HPCL Outage"
            manifestPlaceholders = [
                    appIcon: "@mipmap/hpcl_logo",
                    appIconRound: "@mipmap/hpcl_logo"
            ]
            versionCode 01
            versionName "1.0.0-HPCL"
        }
    }

    defaultConfig {
        applicationId "unistal"
        minSdkVersion 24
        targetSdkVersion 36
    }

    signingConfigs {
        release {
            keyAlias = keystoreProperties['keyAlias']
            keyPassword = keystoreProperties['keyPassword']
            storeFile = keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
            storePassword = keystoreProperties['storePassword']
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            shrinkResources false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    packagingOptions {
        exclude 'META-INF/proguard/androidx-annotations.pro'
        exclude 'META-INF/proguard/coroutines.pro'
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation platform("com.google.firebase:firebase-bom:33.12.0")
    implementation "com.google.firebase:firebase-crashlytics"
    implementation "com.google.firebase:firebase-analytics"
    implementation("com.google.android.play:app-update:2.1.0")
    implementation("com.google.android.play:app-update-ktx:2.1.0")
}
